{
  "id": "presentation.iso_projection",
  "name": "Isometric Projection",
  "category": "presentation",
  "description": "Maps tile coordinates to screen pixels and back. The forward half is the three lines everybody writes; the inverse is the half that gets skipped, and skipping it is why an isometric prototype can draw a world it cannot tell you the mouse is over. Both directions are declared here so they can be held to being exact inverses of each other. `zoom` is a declared INPUT and was only ever read once, at construction, which makes it a number a request sets and a player cannot. Zooming a map is not a rebuild — the world does not change, the lens does — so `setZoom` is published beside it: same number, same clamp, changed live. The origin re-fits on the next frame because it is derived from the viewport and the board rather than declared, which is exactly why it was never an input.",
  "version": "0.1.0",
  "platform": [
    "web"
  ],
  "implementations": [
    {
      "target": "canvas2d",
      "status": "external",
      "module": null,
      "note": "Implemented in the consuming application (generator/iso/IsoProjection.ts); not verifiable from this repository."
    },
    {
      "target": "webgl",
      "status": "unmapped",
      "module": null,
      "note": "A 3D camera does its own projection."
    }
  ],
  "inputs": [
    {
      "name": "tileWidth",
      "type": "number",
      "required": false,
      "default": 64,
      "description": "Screen width of one tile diamond, in pixels."
    },
    {
      "name": "tileHeight",
      "type": "number",
      "required": false,
      "default": 32,
      "description": "Screen height of one tile diamond. The 2:1 ratio against tileWidth is what makes the grid read as isometric rather than merely rotated."
    },
    {
      "name": "elevationPixels",
      "type": "number",
      "required": false,
      "default": 16,
      "description": "Pixels one elevation level lifts a tile up the screen."
    },
    {
      "name": "zoom",
      "type": "number",
      "required": false,
      "default": 1
    }
  ],
  "outputs": [
    {
      "name": "toScreen",
      "type": "function"
    },
    {
      "name": "toTile",
      "type": "function"
    },
    {
      "name": "visibleTileBounds",
      "type": "function"
    },
    {
      "name": "setZoom",
      "type": "object",
      "description": "Set the zoom live, clamped to the same range the input is. It exists because a lens is not a level: changing how close you are looking should not tear down the world and rebuild it, and a rebuild is what a change to a declared input means everywhere else."
    },
    {
      "name": "zoom",
      "type": "number",
      "description": "The current zoom, which after setZoom is not necessarily the one the request asked for. Published so a control can show where it actually is rather than what it last requested."
    }
  ],
  "events": {
    "emits": [
      "presentation.viewportMoved"
    ],
    "listensTo": []
  },
  "dependencies": [],
  "tags": [
    "isometric",
    "projection",
    "rendering",
    "camera",
    "2d",
    "picking"
  ]
}
