{
  "id": "movement.basic_walk",
  "name": "Basic Walk",
  "category": "movement",
  "description": "Ground-based locomotion with configurable speed and acceleration. Movement CONTINUES while a direction is held rather than advancing once per press, because `input.moveDirection` reports whether the key is still down and a body that ignores that makes the player tap to walk. It also accepts `input.faceDirection`, which turns without moving, and `input.aim`, which points the body continuously at a screen position so an attack can be swung or fired along the exact bearing rather than snapped to one of four — the aim half of a twin-stick or the look half of a shoulder-button strafe, and the thing a player needs when the swing scans the way they are pointed and they do not want to step into what they are hitting.",
  "version": "0.1.0",
  "platform": [
    "roblox",
    "godot",
    "agnostic"
  ],
  "implementations": [
    {
      "target": "canvas2d",
      "status": "external",
      "module": null,
      "note": "Implemented in the consuming application; not verifiable from this repository."
    },
    {
      "target": "godot",
      "status": "unmapped",
      "module": null
    },
    {
      "target": "roblox",
      "status": "scaffold",
      "module": "roblox-adapters/movement.basic_walk.adapter.lua"
    }
  ],
  "inputs": [
    {
      "name": "walkSpeed",
      "type": "number",
      "required": false,
      "default": 16,
      "description": "Studs or units per second"
    },
    {
      "name": "acceleration",
      "type": "number",
      "required": false,
      "default": 8
    },
    {
      "name": "startX",
      "type": "number",
      "required": false,
      "default": null,
      "description": "Where the body begins along X, in tiles. Null means the middle of the board. A region entered by walking off a neighbour should begin at the border it was entered THROUGH, not at the centre, or every crossing teleports the player away from the place they were walking toward."
    },
    {
      "name": "startY",
      "type": "number",
      "required": false,
      "default": null,
      "description": "The same along Y. Null means the middle."
    }
  ],
  "outputs": [
    {
      "name": "isMoving",
      "type": "boolean",
      "description": "Whether the character is currently moving"
    },
    {
      "name": "velocity",
      "type": "vector3"
    },
    {
      "name": "aimAngle",
      "type": "number",
      "description": "Where the body is pointed, in radians, measured from due east and increasing clockwise on screen. `input.aim` sets it continuously — a mouse, a stick — while `input.faceDirection` and `input.moveDirection` snap it to the nearest quarter turn. A component that swings along a line needs the angle; one that picks a sprite needs the quarter turn; both are the same fact at different resolutions, which is why this is published beside the four-way facing rather than instead of it."
    }
  ],
  "events": {
    "emits": [
      "movement.started",
      "movement.stopped",
      "movement.blocked"
    ],
    "listensTo": [
      "input.moveDirection",
      "input.faceDirection",
      "input.aim"
    ]
  },
  "dependencies": [],
  "tags": [
    "locomotion",
    "core"
  ]
}
